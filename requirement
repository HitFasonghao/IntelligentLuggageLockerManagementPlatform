相关表有寄存柜表、寄存柜种类表、柜群表
设备管理
- 寄存柜管理界面
	- 页面右上方有一个按钮叫“同步”，点击后提示“是否确认同步所有寄存柜到平台中”，确认后开始同步所有寄存柜
	- 接着是分页查询寄存柜列表，查询条件有设备id、是否分配（待分配、已分配、全部）、寄存柜种类、柜群四个查询条件。设备id对应后端数据库表的device_id字段，是否分配与后端数据库表中的status不对应，而是依据该寄存柜是否有柜群标识，有柜群标识便是已分配，没有柜群标识便是未分配
	- 条件查询框下面是分页展示，不是按照列表的形式，因为字段太少，按照列表展示，太空，每条寄存柜消息是按照一个个方框显示，要美观。方框内展示信息有设备id、是否分配（待分配、已分配）、状态(与数据库status字段对应)、寄存柜种类、柜群，这四个字段在方框内按照从上往下展示。所有的方框从左往右，从上往下排列。
	- 点击方框会显示两种操作，分配种类和分配柜群，已经分配过柜群的不显示这两种操作，而且分配柜群只有在种类分配完之后才能执行成功，否则提示请先设置寄存柜种类
- 寄存柜种类界面
	- 创建寄存柜种类按钮，点击后弹出对话框填写种类信息，包括种类名称、描述、收费金额（自动保留两位小数），收费时间单位（“每小时”，“每半小时”，“每十分钟”），四个字段都是必填，填写完成后点击创建完成创建
	- 条件查询分页列表展示，查询条件有种类名称，收费金额（区间匹配），时间单位
	- 列表显示内容：种类名称、描述、数量（该种类寄存柜的数量，就是在寄存柜管理界面里分配了该种类的寄存柜的总数），收费金额，收费时间单位
	- 列表右侧的操作：显示修改和删除。修改能修改种类名称、描述、收费金额和收费时间单位。删除的话，只能在该种类对应的寄存柜种类数量为0时才能进行删除
- 柜群管理界面
	- 添加柜群按钮，填写柜群名称、柜群地址、经度、纬度和描述，提交时所有字段都不能为空，状态默认设置为forbidden
	- 条件查询分页展示，查询条件柜群名称、柜群地址、状态
	- 柜群信息分页展示也想寄存柜一样，用一个个方框展示，而不是列表，要美观，方框内展示信息有柜群名称、柜群地址和状态
	- 点击柜群信息的方框，跳转到柜群信息详细界面。界面上方显示柜群的详细信息，也就是柜群的各个字段信息，还需要加一个该柜群的寄存柜数量。下方是该柜群的所有寄存柜，也是和寄存柜管理界面一样方框展示，但不需要分页，也不需要条件查询，进入该页面就展示所有寄存柜信息。点击寄存柜信息方框右下侧的图标，显示操作，禁用/启用，删除。禁用只能在寄存柜是启用状态时才能生效，正处于使用状态’opening’,’using’时都不能点，而启用只能在柜群时禁用是才能用。删除只能在柜群是禁用是才能用